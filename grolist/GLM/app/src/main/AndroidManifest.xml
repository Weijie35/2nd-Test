<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SelectTypeForNewItemActivity"></activity>
        <activity android:name=".CreatingNewItemActivity" />
        <activity android:name=".SearchToolActivity" />
        <activity android:name=".QuantityListItemActivity" />
        <activity android:name=".DeleteListItemActivity" />
        <activity android:name=".ListItemActivity" />
        <activity android:name=".DeleteListActivity" />
        <activity android:name=".CreateNewListActivity" /> <!-- <activity android:name=".ListManagerActivity"></activity> -->
        <activity
            android:name=".ListManagerActivity"
            android:label="List Manager"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.qc.seclass.glm.MainActivity" />
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>